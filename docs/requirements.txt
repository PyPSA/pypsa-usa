# Documentation build requirements (ReadTheDocs uses this via .readthedocs.yaml).
# The docs build no longer imports the workflow code (autodoc is disabled), so
# runtime dependencies (pypsa, atlite, snakemake, ...) are intentionally absent.

sphinx>=7
myst-parser>=3
sphinx-book-theme>=1.1
sphinx-copybutton>=0.5
sphinxcontrib-bibtex>=2.6
